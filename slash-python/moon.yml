$schema: "https://moonrepo.dev/schemas/project.json"
language: "python"
tags: ["sdk"]

tasks:
  publish:
    command: "python"
    args: ["-m", "twine", "upload", "dist/*"]
    options:
      runInCI: false
