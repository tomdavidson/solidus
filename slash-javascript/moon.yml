$schema: "https://moonrepo.dev/schemas/project.json"
language: "typescript"
type: "library"
tags: ["sdk", "npm"]

tasks:
  publish:
    command: "pnpm"
    args: ["publish", "--access", "public", "--no-git-checks"]
    options:
      runInCI: false
