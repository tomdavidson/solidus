$schema: "https://moonrepo.dev/schemas/project.json"
language: "rust"
type: "library"
tags: ["sdk"]

tasks:
  publish:
    command: "cargo"
    args: ["publish", "--allow-dirty"]
    options:
      runInCI: false
