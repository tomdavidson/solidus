# .github/workflows/publish-go.yml
name: Validate slash-go

on:
  release:
    types: [published]

jobs:
  validate:
    uses: ./.github/workflows/publish-to-registry.yml
    with:
      tag-prefix: "slash-go/"
      moon-task: "slash-go:test"
    secrets: inherit
